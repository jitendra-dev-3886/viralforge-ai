import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { AlertCircle, Eye, EyeOff, Loader2, Lock, ShieldCheck } from "lucide-react";
import api from "../../api/axios";
import AuthLayout from "../../layouts/AuthLayout";

export default function SetPassword(){const navigate=useNavigate();const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[show,setShow]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState("");const submit=async(e)=>{e.preventDefault();if(password!==confirm){setError("Passwords do not match.");return}setLoading(true);setError("");try{const{data}=await api.post("/auth/set-password",{password});if(!data.success)throw new Error(data.message);navigate("/dashboard",{replace:true})}catch(err){setError(err.response?.data?.detail||err.message||"Unable to update password.")}finally{setLoading(false)}};return <AuthLayout><div className="auth-card"><p className="auth-card__eyebrow">Secure your account</p><h2>Create a local password.</h2><p className="auth-card__intro">Use this password with your email the next time you sign in.</p>{error&&<div className="auth-alert auth-alert--error"><AlertCircle size={17}/><span>{error}</span></div>}<form className="auth-form" onSubmit={submit}><label className="auth-field"><span className="auth-field__label">New password</span><span className="auth-field__control"><Lock size={17}/><input type={show?"text":"password"} minLength={8} value={password} onChange={(e)=>setPassword(e.target.value)} required/><button className="auth-icon-button" type="button" onClick={()=>setShow(!show)} aria-label={show?"Hide password":"Show password"}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></span></label><label className="auth-field"><span className="auth-field__label">Confirm password</span><span className="auth-field__control"><Lock size={17}/><input type={show?"text":"password"} minLength={8} value={confirm} onChange={(e)=>setConfirm(e.target.value)} required/></span></label><button className="auth-submit" disabled={loading}>{loading?<><Loader2 className="animate-spin" size={17}/> Updating...</>:"Save password and continue"}</button></form><p className="auth-security"><ShieldCheck size={14}/> Your provider password is never shared with ViralForge.</p></div></AuthLayout>}
